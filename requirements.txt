# Base dependencies
numpy<2
pandas
tqdm
scikit-learn
librosa
fasttext-wheel

# Syntactic tagging strategy (YAKE)
yake
nltk

# PyTorch y relacionados (CPU only)
--find-links https://download.pytorch.org/whl/torch_stable.html
torch==2.2.1+cpu
torchaudio==2.6.0
transformers[torch]==4.51.2
sentence-transformers==4.0.2
tokenizers==0.21.1

# Whisper ASR - critical for proper language detection
openai-whisper==20240930